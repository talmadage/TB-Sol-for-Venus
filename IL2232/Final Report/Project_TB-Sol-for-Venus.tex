\documentclass[12pt]{article}

% Minimal packages for fast compile
\usepackage{times}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[a4paper,margin=3.5cm]{geometry}
\usepackage{titlesec}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{float}
\usepackage[section]{placeins} % Pour \FloatBarrier
\usepackage{setspace}
\usepackage{hyperref}   % Essential for clickable Table of Contents and links
\usepackage{amsmath}    % For math
\usepackage{amssymb}    % For math symbols

% Hyperlink setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    citecolor=blue,
}

% Section title styles
\titleformat{\section}{\bfseries\fontsize{14}{16}\selectfont}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\bfseries\fontsize{13}{15}\selectfont}{\thesubsection.}{0.5em}{}

% Custom title page
\begin{document}

\begin{titlepage}
\centering
\vspace*{2cm}
\includegraphics[width=0.3\textwidth]{images/KTH_logo.png} % Assurez-vous que le chemin d'accès à l'image est correct.
\vspace{1cm}

{\Huge\bfseries Final Report - TB Sol for Venus\par}
\vspace{2cm}

\begin{flushleft}
\textbf{Name of submission:} Final technical report\\[0.5em]
\textbf{Group name:} Testbench for Bit-Serial RISC-V processor in SoI for Venus\\[0.5em]
\textbf{Date:} 2026/01/06\\[0.5em]
\textbf{Organization:} KTH\\[1.5em]
\textbf{Authors:}\\[0.5em]
\begin{center}
\begin{tabular}{p{5cm}p{6cm}}
\toprule
\textbf{Name} & \textbf{Email} \\
\midrule
Chinmay Purandare & \texttt{chinmayp@kth.se} \\
Lorenzo Parata & \texttt{parata@ug.kth.se} \\
Chongnan Wang & \texttt{chongnan@kth.se} \\
Xiaotian Jiang & \texttt{xjia@kth.se} \\
Xinxin Qin & \texttt{xinxinq@kth.se} \\
Paul Martin & \texttt{paulmart@kth.se} \\
\bottomrule
\end{tabular}
\end{center}
\vspace{1em}
\end{flushleft}

\vfill
\begin{flushright}
\textbf{Department of Electronics}\\
KTH Royal Institute of Technology\\
Stockholm, Sweden
\end{flushright}
\end{titlepage}

\newpage
\thispagestyle{empty} % Ensure no page number on the abstract page

% ==========================================================
% ABSTRACT
% ==========================================================
\section*{Abstract}
\addcontentsline{toc}{section}{Abstract} % Add to Table of Contents without numbering

\noindent

\newpage

% ==========================================================
% TABLE OF CONTENTS
% ==========================================================
\tableofcontents
\newpage

\section*{Acronyms}
\addcontentsline{toc}{section}{Acronyms}

\begin{itemize}
    \item \textbf{RISC-V} Reduced Instruction Set Computer - Five
    \item \textbf{SoI} Silicon-on-Insulator
    \item \textbf{TB} Testbench
    \item \textbf{CPU} Central Processing Unit
    \item \textbf{RAM} Random Access Memory
    \item \textbf{FPGA} Field-Programmable Gate Array
    \item \textbf{I/O} Input/Output
    \item \textbf{Brisc} Bit-Serial RISC-V (Processor Name)
    % Add more acronymes
\end{itemize}

\newpage

\listoffigures
\newpage

\listoftables
\newpage


% ==========================================================
% REPORT BODY
% ==========================================================
\section{Introduction}

The development of electronic systems that resist extreme environments is a real challenge for the space industries. For instance, Venus has constraints on temperature and space radiation that are completely different from Earth. Indeed, conventional technologies are often unsuitable under such conditions, that is why we need alternative design and architectures. 

It is in this context that the project started. It continues previous work at KTH developing digital electronics for extreme high-temperature environments, specifically targeting Venus surface conditions. Last year's students successfully designed and synthesized two RISC-V processor variants—a parallel model in VHDL and a bit-serial CISC-V model in ProGram—both optimized for the 12-pad constraint of KTH's high-temperature test station. However, they did not complete FPGA implementation and testing.

The current project addresses this gap by implementing and testing both processor models on FPGAs, developing an advanced testbench. To do this, we will split the system into two FPGAs: one hosting the processor core, and the other implementing the testbench and external control logic. This work uses KTH's experimental Silicon-on-Insulator technology, which can operate at temperatures approaching Venus's 480°C requirement, and leverages the open-source RISC-V architecture to avoid licensing constraints.

A key aspect of the project is the use of a bit-serial communication interface between the processor and its environment. Thanks to these serialized data transfers, the number of I/O pins can be significantly reduced. It is indeed one of our critical constraints, the system is limited by a maximum of 12 pads for communication between the 2 FPGAs. The reason is test setups for high-temperature that have limited probe access. This architectural choice has direct implications on timing, synchronization, and verification, and therefore plays a central role in both the design and validation phases of the system.

The main phases of the project are:
\begin{itemize}
  \item \textbf{Toolchain and Hardware Setup} : This initial phase involves preparing the necessary software environments and physical hardware for development and testing.
  \item \textbf{FPGA Prototyping of Processor Cores} : This phase focuses on adapting, implementing, and performing initial tests on the existing RISC-V and CISC-V processor models on an FPGA.
  \item \textbf{Testbench Development} : This involves designing and implementing the advanced testbench on a separate FPGA, which will be used to validate the processor cores.
  \item \textbf{System Integration and Verification} : This phase covers the physical connection of the two FPGAs and the execution of comprehensive tests to validate the complete system and debug any potential issues.
  \item \textbf{Project Finalization and Documentation} : The final phase includes creating detailed documentation for the designs and test procedures, and preparing the final project deliverables.
\end{itemize}

The report is structured as follows. The first part presents the synthesis results of the processor, including an overview of the synthesis flow, timing analysis, and the internal structure of the FPGA hosting the CPU. Indeed, the first task of our project was to verify both CPU programs, using Quartus and Modelsim. The second part describes the testbench architecture implemented on the second FPGA. We will see the testbench itself with the different tests we wrote but also the serialization mechanism used to interface with the processor. The third part details the communication scheme between the two FPGAs. This part will highlight the communication module and protocol but also the synchronization challenges especially with the clock gestion. We will finally conclude the report with a summary of the achieved results and outlines possible directions for future work. 

\newpage
\FloatBarrier
\section{Synthesis}

\subsection{Difference between RiscV, CiscV and BriscV}

\subsection{Synthesis Overview}

\subsubsection{Modelsim Simulation}
\subsubsection{Implementation on FPGA}

\subsection{CPU Timing Analysis}

Now that we are sure both CPUs are synthesized and can be run on FPGA. We can perform a timing analysis of the different processor cores. As we said, all processors were synthesized using Quartus, and we will use the same tool for the analysis. However, the initial synthesis results did not include any timing analysis because Quartus was unable to properly identify and constrain the clock domain of the design.

The root cause of this issue was that the internal clock signal of the processor cores (\texttt{clk}) was not directly connected to the physical clock input of the FPGA board (\texttt{FPGA\_CLK\_50}). As a consequence, Quartus couldn’t generate any timing report. To resolve this problem, a wrapper module was introduced and defined as the new top-level entity of the design. This wrapper explicitly connects the FPGA hardware signals, including the board clock, reset signal, and output peripherals such as LEDs, to the internal processor core. As a result, the processor clock is driven directly by the \texttt{FPGA\_CLK\_50} signal. In addition, a simple sequential process was added inside the wrapper to drive a blinking LED. This step was necessary so Quartus doesn’t optimize away the entire design during synthesis. Indeed, without observable logic driven by the clock, the synthesis tool may consider parts of the design as redundant and decide to not use them. 

After that, Quartus successfully generated timing analysis reports for the processor cores. However, the result showed negative setup slack values. It means that the design did not meet the default timing assumptions. This result is totally normal since no explicit timing constraints had been defined for the clock signal. So we wrote a Synopsys Design Constraints (SDC) file to properly constrain the clock period at 20ns. After applying the SDC constraints, the timing violations were resolved and the designs met the desired timing requirements. Table~\ref{tab:risc_timing} summarizes the observed setup and hold slack values for the RISC processor before and after applying the SDC constraints.

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
 & Setup Slack (ns) & Hold Slack (ns) \\
\hline
Without SDC & $\approx -1$ & $\approx 0.3$ \\
With SDC    & $\approx 11$ & $\approx 0.3$ \\
\hline
\end{tabular}
\caption{Timing analysis results for the RISC processor}
\label{tab:risc_timing}
\end{table}


As shown in the table, the application of clock constraints significantly improved the setup slack that is now positive, it confirms the validity of the processor. Quartus also showed us several cases, especially the worst case scenario at min and max temperature, and in all cases the slack remained positive.

The same methodology was applied to the BRISC processor and it shows the same result. As a result, this part validates the feasibility of both processor designs under realistic clocking conditions (20ns).


\subsection{FPGA1 (CPU) Structure}


\FloatBarrier 
\section{Testbench}


\subsection{FPGA2 (TB) Structure}

\subsection{TB Serialization - Bit Serial Interface}



\FloatBarrier
\section{FPGA Communication} 



\FloatBarrier
\section{Conclusion}

\subsection{Conclusion}

\subsection{Future Work}


% ==========================================================
% REFERENCES 
% ==========================================================
\newpage
\section{References}

\begin{thebibliography}{99}
    % [1] Author(s), "Title of work", Publisher/Journal, Year.
    \bibitem{KTH} KTH Royal Institute of Technology, Official Website. \url{https://www.kth.se}
    \bibitem{SoI} Smith, J. et al. "High-Temperature Characteristics of SoI CMOS for Venus Missions," Journal of Extreme Environment Electronics, 2024.
\end{thebibliography}

\end{document}